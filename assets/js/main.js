/**
 * Dummy Architect Site — testing only.
 * Minimal JS for any future enhancements (e.g. nav toggle on small screens).
 */
(function () {
  'use strict';
  // Optional: add data attribute for automation
  if (document.documentElement && !document.documentElement.getAttribute('data-test-site')) {
    document.documentElement.setAttribute('data-test-site', 'true');
  }
})();
